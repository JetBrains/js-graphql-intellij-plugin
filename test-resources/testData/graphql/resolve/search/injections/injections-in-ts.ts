declare const isEnabled: boolean;

const someStr = `abc`;
const someStr1 = `abc`;

if (Math.random() > 0.5) {
  while (isEnabled) {
    const FRAGMENT = gql`
      fragment SomeFragmentInTs on User {
        name
      }
    `
  }
}