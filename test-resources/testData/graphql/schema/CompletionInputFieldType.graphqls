type KnownType {
    k: String
}

interface KnownInterface {
    i: Boolean
}

enum MyEnum {
    A, B, C
}

union MyUnion = KnownType | KnownInterface

input MyInput1 {
    foo: String
}

input MyCompletionInput {
    bas: <caret>
}