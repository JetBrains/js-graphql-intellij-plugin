type Query {}

input User @one @two {
  name: Int
  id: ID
  phone: String
}

extend input User {
  address: String
}

extend input User @three {}

extend input User {}

extend input User {
  zip: String
}

