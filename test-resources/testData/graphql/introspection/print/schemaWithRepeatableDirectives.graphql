schema {
    query: Query
}

directive @cache repeatable on FIELD

directive @format(pattern: String) on SCALAR

directive @tableField(name: String!) repeatable on FIELD | FIELD_DEFINITION

type Query {
    id: ID
}
