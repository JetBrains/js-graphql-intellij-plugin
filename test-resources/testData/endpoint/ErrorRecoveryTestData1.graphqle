type Foo implements  {
    foo: String
}

type Foo implements B,  {
    foo: String
}

invalid

interface Bar {
    incomplete:
}

type Foo2

union U3 = A | B |

type Foo3 {

    @Ann()
    ann: A

    @Ann(foo =)
    ann: A

    @Ann(foo = true, )
    ann: A

    @Ann(foo = true, bar = invalid)
    ann: A

    @B
    bann: String
}