type KnownType {
    k: String
}

interface KnownInterface {
    i: Boolean
}

type More implements <caret>

enum MyEnum {
    A, B, C
}

union MyUnion = KnownType | KnownInterface

# comment

type Foo implements Bar {
    foo:
}
